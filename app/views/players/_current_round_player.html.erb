<div class="player">
  <%= current_round_player.name %> (<%= describe_pile_size current_round_player.hand_size %>)
  <ul class="cards">
    <% current_round_player.hand.each do |card| %>
      <li class="card"><%= card %></li>
    <% end %>
  </ul>
</div>
